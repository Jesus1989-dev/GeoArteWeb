import { RegistroController } from "@/components/features/auth/RegistroController";

export const metadata = {
  title: "Crear cuenta",
};

export default function RegistroPage() {
  return <RegistroController />;
}
